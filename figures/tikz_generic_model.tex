\begin{tikzpicture}[
	node distance=2cm,
	set/.style={draw, circle, minimum size=2cm},
	randomvar/.style={draw, rectangle, minimum size=1.5cm},
	arrow/.style={->, >=stealth, shorten >=2pt, shorten <=2pt}
	]
	% Sample Space Omega
	
	\node[set, label={above:$\Omega_X$}] (omega) {$\omega_x$};
	\node[set, right=of omega, label={above:$\mathbb{X}$}] (X) {x};
	\node[set, below=of omega, label={left:$\mathcal{F}_X$}] (F) {$\sim 2^{\Omega_X}$};
	
	\node[set, below= of F, label={left:$\mathcal{F}$}] (Ftot) {$\sim 2^\Omega$};
	\node[set, below=of Ftot, label={left:$\mathcal{F}_Y$}] (FY) {$\sim 2^{\Omega_Y}$};
	\node[set, below= of FY, label={below:$\Omega_Y$}] (omegaY) {$\omega_Y$};
	
	\node[set, right=of omegaY, label={below:$\mathbb{Y}$}] (Y) {y};
	\node[right = 3cm of Ftot] (unit) {$[0,1]$};
	\node[right = 4cm of unit] (P1) {$P_\theta$};
	
	
	\node[set, below = of P1, label={above:$\mathcal{P}$}] (P) {$P_\theta$};		
	\node[set, right = of X, label={above:$G$}] (G) {$\theta$};
	\node[set, right = of G, label={above:$\Gamma$}] (gamma) {$\gamma$};
	
	\node[right = 4cm of P1] (R) {$\mathbb{R}_{\geq 0}$};
	
	\draw[arrow] (gamma) -- node[above] {$\Theta: \Gamma \mapsto G$} (G);
	
	\draw[arrow] (X) -- node[above] {} (P1);
	\draw[arrow] (Y) -- node[above] {} (P1);
	\draw[arrow] (G) -- node[above] {} (P1);
	\draw[arrow] (P) -- node[above] {} (P1);
	
	
	\draw[arrow] (F) -- node[right = 3mm] {} (Ftot);
	\draw[arrow] (FY) -- node[right = 3mm] {} (Ftot);
	\draw[arrow] (Ftot) -- node[above = 3mm] {$\mathbb{P}: \mathcal{F} \mapsto [0,1]$} (unit);
	\draw[arrow] (F) -- node[right = 3mm] {$\mathbb{P}_X: \mathcal{F}_X \mapsto [0,1]$} (unit);
	\draw[arrow] (FY) -- node[right= 3mm] {$\mathbb{P}_Y: \mathcal{F}_Y \mapsto [0,1]$} (unit);
	
	\draw[arrow] (P1) -- node[above = 2mm] {$p: \mathbb{X}\times \mathbb{Y}\times G \mapsto [0,1]$} (unit);
	
	
	\draw[arrow] (P1) -- node[above] {$f: \mathbb{X}\times \mathbb{Y}\times G \mapsto [0,\infty)$} (R);
	\draw[arrow] (omega) -- node[above] {} (F);
	\draw[arrow] (omegaY) -- node[above] {} (FY);
	\draw[arrow] (omega) -- node[above] {$X: \Omega \mapsto \mathbb{X}$} (X);
	\draw[arrow] (omegaY) -- node[above] {$Y: \Omega_Y \mapsto \mathbb{Y}$} (Y);
	
	\draw[dashed] (12.1,0) ellipse (3 and 1.5);
	
	% Add text above the oval
	\node[above] at (12.1,1.5) {Bayesian addition};
	
\end{tikzpicture}